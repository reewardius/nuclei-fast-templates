id: azure-bucket

info:
  name: Azure Bucket Detect 
  author: reewardius
  severity: info
  tags: android
  
file:
  - extensions:
      - all
      
    extractors:
      - type: regex
        regex:
          - "[a-z0-9.-]+\\.blob\\.core\\.windows\\.net"